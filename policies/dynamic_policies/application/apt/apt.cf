bundle agent apt {

vars:
    "bundles[dummy]"    string => "dummy.cf";


    "sequence"          slist  => getindices("bundles");
    "input"             slist  => getvalues("bundles");

methods:

 "any" usebundle => "${roadshow.os}_${roadshow.application}";

}
