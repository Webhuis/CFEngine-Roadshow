bundle agent nfsutl {

vars:

  "bundles[nfs_server]"                string => "nfs_server.cf";

  "sequence"                	        slist => getindices("bundles");
  "inputs"                              slist => getvalues("bundles");

methods:

  "We desire the nfs kernel server" usebundle => "nfs_server";

}
