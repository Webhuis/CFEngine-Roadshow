bundle agent Data_common {

files:

   "$(sys.workdir)/bin/cf-messaged.py"
      handle => "update_Data_listener",
      copy_from => u_rcp("${master_dynamic}/bin/cf-messaged.py", ${sys.policy_hub}),
      action => u_immediate,
      classes => u_if_repaired("update_report");

}
