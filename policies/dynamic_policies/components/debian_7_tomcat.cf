bundle agent debian_7_tomcat {

vars:

  "pkg[nfs-common]"        string => "*";
  "pkg[openjdk-6-jre]"     string => "*";
  "pkg[tomcat6]"           string => "*";

methods:

  "any" usebundle => packages("debian_7_tomcat.pkg");

commands:

 restart_tomcat::
  "/etc/init.d/tomcat6 restart"
     handle => "restart_tomcat",
    comment => "restarting tomcat";

}
