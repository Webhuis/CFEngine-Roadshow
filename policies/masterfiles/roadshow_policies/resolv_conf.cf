bundle agent resolv_conf
{
  methods:
      "" usebundle => file_make_mog("/etc/resolv.conf", "
search ${sys.domain}

nameserver ${${do_roadshow.class_domain}.nameserver}", 644, "root", "root"),
      comment => "Domain resolv.conf door do_roadshow.class_domain ${do_roadshow.class_domain}";
}
