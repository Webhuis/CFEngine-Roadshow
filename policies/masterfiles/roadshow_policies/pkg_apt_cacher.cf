bundle agent pkg_apt_cacher
{
  vars:
      "pkg[apt-cacher-ng]"               string => "*";

  methods:
      # this bundle is not defined? -tzz
      # "any" usebundle => apt_packages("pkg_apt_cacher.pkg");

}
