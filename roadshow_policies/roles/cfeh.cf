bundle agent cfeh {

vars:

  "bundles[cfe_hub]"      string => "cfe_hub.cf";

  "sequence"  slist => getindices("bundles");
  "inputs"    slist => getvalues("bundles");

methods:

  "any" usebundle => "cfe_hub";

}
