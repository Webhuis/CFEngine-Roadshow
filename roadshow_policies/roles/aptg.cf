bundle agent aptutl {

vars:

  "role_name"                         string => "apt-cache-ng common cacher for Debian and rpm packages";

  "pkg[apt-cacher-ng]"           string => "*";

methods:

  "any" usebundle => packages("aptutl.pkg");

}
