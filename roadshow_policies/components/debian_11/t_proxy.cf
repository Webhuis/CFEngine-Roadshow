bundle agent t_proxy {

vars:

  "t_proxy.cf"              string => "I am a Transparent Proxy"; 

  "pkg[fail2ban]"           string => "*";

methods:

  "We IP filtering"      usebundle => packages("gateway_tools.pkg");

reports:

  "${this.bundle}: ";

}
