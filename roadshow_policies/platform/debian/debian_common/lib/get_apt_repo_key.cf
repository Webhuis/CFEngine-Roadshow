bundle agent get_apt_repo_key(apt_key_name) {

commands:

# "/usr/bin/wget -O /tmp/$(apt_key_name) http://support-svn.webhuis.nl/gpg-keys/$(apt_key_name)";
  "/usr/bin/wget -O /tmp/$(apt_key_name) http://pdnmwhpdcrg2.rg.webhuis.nl/mirror/gpg-keys/$(apt_key_name)";

reports:

  "$(this.bundle):";

}

