bundle agent ddns_update (ddns_name, mac_address, ip_address) {

vars:

  "naam"                                         string => $(ddns_name);

#files:

#methods:

  #"We update the isc-dhcp-server-ldap lda"    usebundle => dhcp_ldap_update;
  #"We dynamically update the name server"     usebundle => ddns_update,
  #                                              comment => "We make ourselves known";

reports:

  "$(this.bundle): $(naam)";

}

