bundle agent get_mac_address(interface) {
  
classes:

  "ok" expression => regextract(
                                ".*HWaddr ([^\s]+).*(\n.*)*",
                                "$(interface)",
                                "mac_address");
reports:

 "$(this.bundle): $(interface), $(mac_address[1])";

}

