bundle common stop_ip {

classes:

  "stop_ip"       expression => regcmp( "bad", "$(status_device.device.status)" );

reports:

  "$(this.bundle): $(status_device.device.status)" );

 stop_ip:: 
  "$(this.bundle): stop configuring network ip";

}

