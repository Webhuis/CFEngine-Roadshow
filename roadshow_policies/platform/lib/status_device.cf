bundle agent status_device(status) {

classes:

  "stop"                 expression => regcmp( "bad", "$(status)");

methods:

 stop::
  "Stop IP processing"    usebundle => "stop_ip";

reports:

  "$(this.bundle) device: $(device) $(status_device)";

}
