bundle agent nic {

vars:

  "nic_index"          slist  => getindices("sys.ipv4");
  "nic_values"         slist  => getvalues("sys.ipv4");

reports:

  "${this.bundle} nics: $(nic_index) $(sys.ipv4[nic_index])";

}
