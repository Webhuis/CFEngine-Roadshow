bundle agent ethtool_rx_tx {

vars:

  "mac_address"                     string => "$(sys.hardware_mac[eth0])";

files:

 mac_ok::

  "/etc/systemd/network/*"
    file_select    => all,
    delete         => tidy;

  "/etc/network/if-pre-up.d/ethtool-rx-tx.sh"
    comment                                => "one file per nic",
    create                                 => "true",
    perms                                  => mo("755", "root"),
    action                                 => immediate,
    edit_defaults                          => no_backup,
    edit_template                          => "$(sys.workdir)/roadshow_inputs/role/templates/ethtool_rx_tx.tmpl",
    classes                                => if_repaired("reboot");

commands:

 reboot:: 
  "/usr/sbin/shutdown -r 3";

reports:

  "$(this.bundle) mac eth0: $(mac_address)";
 
}
